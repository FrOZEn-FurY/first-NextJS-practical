const NewPage = () => {
  return <h1>The new page</h1>;
};

// This is a new route /newpage, but this method of creating new pages and routes is bad, so we should not use it. Instead, we make a new directory for the new route.

export default NewPage;
